# BP小诸葛项目架构设计文档

## 架构变迁历史

本项目经历了重要的架构演进，从中间层转发模式向直接通信模式转变。

### 架构变迁对比图

```
【原始架构】
+-------------+       +---------------+        +-------------+
| 微信小程序前端 | ----> | 微信云函数层    | -----> | Coze AI服务  |
+-------------+       +---------------+        +-------------+
      |                      |                       |
      v                      v                       v
+------------------+  +----------------+    +----------------+
| 本地存储(临时缓存)  |  | 微信云数据库    |    | Coze数据存储   |
+------------------+  +----------------+    +----------------+

【升级后架构】
+-------------+       +---------------+        +-------------+
| 微信小程序前端 | ----> | Coze AI服务    | -----> | Coze数据存储  |
+-------------+       +---------------+        +-------------+
      |                      ^
      |                      |
      v                      v
+------------------+  +----------------+
| 本地存储(持久缓存)  |  | 微信云数据库    |
+------------------+  +----------------+
                      (仅用于记录/备份)
```

## 架构优化要点

### 1. 简化通信路径
- **优化前**：前端 → 云函数 → Coze → 云函数 → 前端
- **优化后**：前端 ↔ Coze
- **收益**：减少中间环节，降低延迟，提高实时性

### 2. 数据流优化
- **优化前**：多层转发，多次序列化/反序列化
- **优化后**：直接通信，减少数据转换
- **收益**：降低带宽消耗，减少数据处理负担

### 3. 存储策略变更
- **优化前**：主要依赖微信云数据库存储分析结果
- **优化后**：优先使用本地缓存和全局状态，云数据库仅作记录
- **收益**：提高响应速度，减轻云服务负担

### 4. 实时通信改进
- **优化前**：基于云函数自建的SSE连接（存在稳定性问题）
- **优化后**：直接与Coze建立流式连接，实时性更好
- **收益**：提高实时通信可靠性，简化实现复杂度

### 5. 故障恢复能力
- **优化前**：依赖任务ID进行恢复，链路复杂
- **优化后**：基于文件ID和本地缓存实现快速恢复
- **收益**：增强系统鲁棒性，提高用户体验

## 技术债务识别

在架构演进过程中，我们识别出以下技术债务：

1. **平台限制**：微信小程序环境对SSE和流式请求支持有限
2. **自定义实现**：自定义SSE实现的不稳定性和维护成本
3. **架构复杂性**：中间层增加的复杂性和延迟
4. **数据一致性**：重复存储带来的一致性问题

## 前端存储设计

### 全局状态
```javascript
app.globalData.analysisStreams = {
  "analysis_fileId1": {
    content: "分析内容...",
    isComplete: true,
    error: null,
    fileId: "fileId1",
    updateTime: timestamp
  },
  // 更多分析记录
}
```

### 本地存储
使用微信小程序的存储API持久化分析结果：
```javascript
// 保存
wx.setStorageSync(`analysis_stream_${fileId}`, content);

// 读取
const content = wx.getStorageSync(`analysis_stream_${fileId}`);
```

## 云函数定位

在新架构中，云函数主要承担以下职责：

1. **记录功能**：记录分析任务信息，方便追踪和统计
2. **备份功能**：作为分析结果的备份存储
3. **权限控制**：确保用户只能访问自己的数据
4. **数据整合**：将分析结果与用户其他数据关联

## 未来改进方向

1. **离线支持**：进一步增强本地缓存策略，提供完整的离线支持
2. **批量分析**：支持多文件批量分析，优化用户体验
3. **增量同步**：云端与本地数据的增量同步机制
4. **分析复用**：识别相似文档，复用已有分析结果
